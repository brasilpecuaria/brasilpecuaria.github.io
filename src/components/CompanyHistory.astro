---
const timeline = [
  {
    year: "2025",
    title: "Fundação",
    description:
      "Pecuária Brasil foi fundada com a visão de revolucionar práticas sustentáveis de pecuária no Brasil. Nossos fundadores trouxeram décadas de experiência combinada em agricultura e ciência ambiental.",
  },
  {
    year: "2026",
    title: "Presente e Futuro",
    description:
      "Agora atendemos X fazendas na regiao Centro-Oeste. Comprometidos em trazer insights orientados indústria de pecuária.",
  },
];
---

<section class="py-16 bg-gray-50">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <h2 class="text-3xl font-bold text-center text-gray-900 mb-12">
      Nossa Trajetória
    </h2>

    <div class="space-y-8">
      {timeline.map((item, index) => (
        <div class="flex gap-8">
          <div class="flex flex-col items-center">
            <div class="w-12 h-12 bg-green-700 text-white rounded-full flex items-center justify-center font-bold text-lg">
              {index + 1}
            </div>
            {index < timeline.length - 1 && (
              <div class="w-1 h-32 bg-green-200"></div>
            )}
          </div>
          <div class={index < timeline.length - 1 ? "pb-8" : ""}>
            <h3 class="text-2xl font-bold text-green-700 mb-2">
              {item.year} - {item.title}
            </h3>
            <p class="text-gray-700">{item.description}</p>
          </div>
        </div>
      ))}
    </div>
  </div>
</section>
