---
import { Image } from 'astro:assets';
---

<section id="home" class="relative w-full min-h-screen bg-white py-12 sm:py-16 lg:py-20">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    {/* Container with split layout */}
    <div class="grid grid-cols-1 lg:grid-cols-2 gap-8 lg:gap-12 items-center">
      {/* Left side - Text content */}
      <div class="flex flex-col justify-center">
        <h1 class="text-4xl sm:text-5xl lg:text-6xl font-bold text-gray-900 mb-6 leading-tight">
          Transformando a Pecuária Brasileira
        </h1>
        <p class="text-lg sm:text-xl text-gray-600 mb-8 leading-relaxed">
          Práticas sustentáveis de pecuária que aumentam produtividade, protegem nosso ambiente e criam um futuro melhor para gerações vindouras.
        </p>
        <div>
          <a 
            href="#contact" 
            class="inline-block px-8 py-4 bg-green-700 text-white font-semibold rounded-lg hover:bg-green-800 transition duration-200 shadow-md hover:shadow-lg"
          >
            Contato
          </a>
        </div>
      </div>

      {/* Right side - Image placeholder */}
      <div class="flex items-center justify-center">
        <div class="w-full aspect-square bg-gradient-to-br from-green-100 to-green-200 rounded-lg shadow-lg flex items-center justify-center overflow-hidden">
          {/* Placeholder with gradient and icon */}

        <Image src="/images/generated_hero_img.png" alt="Hero" class="mr-2" width="500" height="500" />
        </div>
      </div>
    </div>
  </div>
</section>
